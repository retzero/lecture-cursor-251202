```
너는 유능한 파이썬 소프트웨어 개발자야. 동료의 퇴사로 동료가 담당하던 코드를 담당하게 되었어. @employee_salary.py 의 기능을 상세하게 설명해줘. 주요 기능을 먼저 분석하여 설명하고 상세 기능을 설명해줘. 각 클래스의 속성과 메서드, 그리고 함수의 기능을 각각 설명해주면 좋겠어.
```

```
너는 유능한 파이썬 소프트웨어 개발자야. @employee_salary.py의 클래스 및 함수를 검증할 수 있는 테스트케이스를 생성해줘. Salary가 음수인 경우 및 type이 음수인 경우도 고려해서 테스트케이스를 만들어줘. 결과는 test_employee_salary.py에 테스트케이스 코드를 추가하고 주석은 한글로 설명해주면 좋겠어.
```

```
너는 유능한 파이썬 소프트웨어 개발자야. 해외연구소의 동료가 개발한 코드를 전달받아서 개선하게 되었어. @inventory_report.py 의 기능을 상세하게 설명해줘. 주요 기능을 먼저 분석하여 설명하고 상세 기능을 설명해줘. 각 클래스의 속성과 메서드, 그리고 함수의 기능을 각각 설명해주면 좋겠어. README.md에 markdown format으로 작성해줘. 파일은 small chunks로 편집해줘.
```

```
## 역할
너는 유능한 파이썬 소프트웨어 개발자야. 해외연구소의 동료가 개발한 코드를 전달받아서 개선하게 되었어.

## 코드 분석 요청
다음 항목들을 종합적으로 분석해주세요. 코드 수정은 하지 마세요.

### 1. 코드 품질 분석
- [ ] 코드 구조 및 설계 패턴 검토 (중복 코드, 상속/다형성 활용 가능성)
- [ ] 가독성 및 유지보수성 평가 (변수명, 주석, 함수 분리)
- [ ] 에러 처리 및 예외 상황 대응 여부 확인
- [ ] 매직 넘버 제거 및 상수화 필요성 검토
- [ ] 타입 힌팅 및 문서화(docstring) 추가 필요성 평가

### 2. 리팩토링 검토
- [ ] 중복 코드 제거
- [ ] 클래스 설계 개선 (상속 구조 또는 공통 베이스 클래스 도입)
- [ ] 가격 계산 로직의 명확성 및 테스트 용이성 향상
- [ ] 매개변수 검증 및 입력값 유효성 검사 추가

### 3. 검토 우선순위
1. **높음**: 중복 코드 제거 및 공통 로직 추출
2. **높음**: 매직 넘버 상수화 및 타입 안정성 향상
3. **중간**: 테스트 케이스 추가 및 테스트 커버리지 확보
4. **중간**: 에러 처리 및 입력값 검증 추가
5. **낮음**: 문서화 개선

## 출력방식
- README.md에 markdown format으로 작성해줘.
- 파일은 small chunks로 편집해줘.해줘.
```


```
[요청사항]
아래 조건에 따라 Markdown 형식으로 다양한 경우의 예제 테이블을 작성하세요.
[조건]
- 렌터카의 가격은 자동차 유형, 고객 등급, 보험 등급, 대여 기간에 따라 결정됩니다.
- 자동차 유형은 Compact와 Van의 두 종류가 있습니다.
- 고객 등급은 Standard와 VIP의 두 종류가 있습니다.
- 보험 등급은 Standard와 Full cover의 두 종류가 있습니다.
- 대여 기간은 대여일(days)로 합니다.
- 렌터가 가격에는 10%의 부가가치세(VAT)가 더해집니다.
- VIP 고객에게는 할인이 적용됩니다.
- 기능: 고객 및 렌터가 유형에 따른 대여 가격 계산
```

```
## 역할
당신은 유능한 파이썬 테스트 엔지니어입니다. 렌터카 가격 계산 시스템을 위한 포괄적인 단위 테스트 케이스를 작성해야 합니다.

## 코드 구조
다음 두 개의 클래스가 있습니다:

### StandardCustomer 클래스
### VipCustomer 클래스

## 가격 계산 규칙

### 자동차 일일 렌탈 가격
- Compact (100): $50/일
- Van (200): $90/일

### 보험 일일 가격
- Compact + Standard (0): $6/일
- Compact + Full cover (1): $13/일
- Van + Standard (0): $14/일
- Van + Full cover (1): $20/일

### 세금 및 할인
- Standard 고객: (자동차 가격 + 보험 가격) × 1.1 (VAT 10%)
- VIP 고객: (자동차 가격 + 보험 가격) × 1.1 × 0.8 (VAT 10% + 20% 할인)
- 최종 가격은 `int()`로 변환되어 소수점 버림

## 테스트케이스 요구사항

### 1. StandardCustomer 테스트
#### 1.1 기본 가격 계산 테스트
#### 1.2 get_car_type_name() 테스트
#### 1.3 report() 테스트

### 2. VipCustomer 테스트
#### 2.1 VIP 할인 적용 테스트
#### 2.2 report() 테스트

### 3. 엣지 케이스 테스트
#### 3.1 경계값 테스트
#### 3.2 잘못된 입력값 테스트

### 4. 통합 테스트
- `rental_cars.py`의 `main()` 함수 실행 시 예상 출력과 일치하는지 확인

## 테스트 프레임워크
- `pytest` 사용
- 각 테스트는 독립적으로 실행 가능해야 함
- 테스트 함수명은 명확하고 설명적이어야 함

## 출력 형식
다음 형식으로 테스트 파일을 작성하세요:
- 파일명: `test_rental_cars.py`
- 각 테스트 클래스는 `TestStandardCustomer`, `TestVipCustomer`로 분리
- 각 테스트 메서드는 `test_` 접두사 사용
- Assertion 메시지는 명확하게 작성

## 추가 요구사항
1. **테스트 커버리지**: 모든 메서드와 주요 분기 경로를 커버해야 함
2. **테스트 데이터**: 다양한 조합의 입력값 사용

위의 요구사항에 따라 포괄적인 테스트 케이스를 작성해주세요.
```


```
## 역할
너는 유능한 파이썬 소프트웨어 개발자야. 해외연구소의 동료가 개발한 코드를 전달받아서 개선하게 되었어.

## 코드 분석 요청
다음 항목들을 종합적으로 분석해주세요. 코드 수정은 하지 마세요.

### 1. 코드 품질 분석
- [ ] 코드 구조 및 설계 패턴 검토 (중복 코드, 상속/다형성 활용 가능성)
- [ ] 가독성 및 유지보수성 평가 (변수명, 주석, 함수 분리)
- [ ] 에러 처리 및 예외 상황 대응 여부 확인
- [ ] 매직 넘버 제거 및 상수화 필요성 검토
- [ ] 타입 힌팅 및 문서화(docstring) 추가 필요성 평가

### 2. 리팩토링 검토
- [ ] 중복 코드 제거
- [ ] 클래스 설계 개선 (상속 구조 또는 공통 베이스 클래스 도입)
- [ ] 가격 계산 로직의 명확성 및 테스트 용이성 향상
- [ ] 매개변수 검증 및 입력값 유효성 검사 추가

### 3. 검토 우선순위
1. **높음**: 중복 코드 제거 및 공통 로직 추출
2. **높음**: 매직 넘버 상수화 및 타입 안정성 향상
3. **중간**: 테스트 케이스 추가 및 테스트 커버리지 확보
4. **중간**: 에러 처리 및 입력값 검증 추가
5. **낮음**: 문서화 개선

## 출력방식
- README.md에 markdown format으로 작성해줘.
- 파일은 small chunks로 편집해줘.
```


```
** 코드 품질 관점의 리뷰 프롬프트 **

코드를 검토하고 아래 체크리스트를 기준으로 리뷰해주세요:

## 필수 체크 항목
- [ ] PEP 8 스타일 가이드 준수
- [ ] 함수/변수명이 명확하고 의미 있는가?
- [ ] 타입 힌트가 적절히 사용되었는가?
- [ ] docstring이 모든 공개 함수/클래스에 있는가?
- [ ] 예외 처리가 적절한가?
- [ ] 입력값 검증이 있는가?
- [ ] 코드 중복이 없는가?
- [ ] 순환 복잡도가 적절한가? (10 이하 권장)
- [ ] 함수 길이가 적절한가? (50줄 이하 권장)
- [ ] 매직 넘버/문자열이 없는가?

## 권장 체크 항목
- [ ] 로깅이 적절히 사용되었는가?
- [ ] 설정값이 하드코딩되지 않았는가?
- [ ] 테스트 코드가 작성 가능한 구조인가?
- [ ] 의존성이 명확한가?
- [ ] 에러 메시지가 명확한가?

## 참고사항
- 코드 리뷰 시 긍정적인 피드백도 포함하는 것이 좋습니다
- 구체적이고 실행 가능한 제안을 제공하세요
- 코드의 맥락과 목적을 고려하여 리뷰하세요
- 개선 제안 시 대안을 함께 제시하세요
```


```
** 아키텍처 및 디자인 패턴 관점의 리뷰 프롬프트 **
코드를 아키텍처, 디자인 패턴, 코드 구조 관점에서 리뷰해주세요.
다음 항목들을 중심으로 평가해주세요:

## 아키텍처 관점
- [ ] 레이어 분리: 비즈니스 로직, 데이터 접근, 프레젠테이션 레이어가 적절히 분리되었는가?
- [ ] 의존성 방향: 의존성이 올바른 방향으로 흐르는가? (고수준 모듈이 저수준 모듈에 의존하지 않는가?)
- [ ] 모듈화: 기능별로 적절히 모듈화되어 있는가?
- [ ] 관심사의 분리 (Separation of Concerns): 각 모듈/클래스가 단일 책임을 가지는가?
- [ ] 결합도와 응집도: 모듈 간 결합도는 낮고, 모듈 내 응집도는 높은가?

## 디자인 패턴 관점
- [ ] 적절한 패턴 사용: 현재 상황에 맞는 디자인 패턴이 적용되었는가?
- [ ] 안티패턴 회피: God Object, Spaghetti Code, Magic Numbers 등 안티패턴이 없는가?
- [ ] 패턴 과용: 불필요하게 복잡한 패턴을 사용하지 않았는가?
- [ ] 일반적으로 적용 가능한 패턴:
* Factory Pattern: 객체 생성 로직 캡슐화
* Strategy Pattern: 알고리즘 교체 가능성
* Observer Pattern: 이벤트 기반 통신
* Repository Pattern: 데이터 접근 추상화
* Dependency Injection: 의존성 주입
* Singleton Pattern: 단일 인스턴스 보장 (신중하게 사용)

## 코드 구조 관점
- [ ] 클래스 구조: 클래스가 적절한 크기와 책임을 가지는가?
- [ ] 함수 구조: 함수가 단일 책임을 가지며 적절한 길이인가?
- [ ] 상속 vs 컴포지션: 상속이 적절히 사용되었는가? 컴포지션이 더 나은 경우는 없는가?
- [ ] 인터페이스/추상 클래스: 공통 인터페이스가 적절히 정의되어 있는가?
- [ ] 네이밍: 클래스, 함수, 변수명이 의도를 명확히 표현하는가?

## 구체적인 개선 제안
- [ ] 발견된 문제점과 개선 방안
- [ ] 리팩토링 제안
- [ ] 대안 아키텍처나 패턴 제시
```



```
** 성능 최적화 관점의 리뷰 프롬프트 **
코드를 성능 최적화 관점에서 코드 리뷰를 해주세요. 다음 항목들을 중점적으로 검토해주세요:

## 1. 알고리즘 복잡도 분석
- 시간 복잡도(Time Complexity): 현재 알고리즘의 Big-O 표기법을 분석하고, 더 효율적인 알고리즘이 있는지 확인
- 공간 복잡도(Space Complexity): 메모리 사용량이 최적화되어 있는지 확인
- 불필요한 중첩 루프나 반복 연산이 있는지 확인
- 조기 종료(early exit) 조건이 적절히 사용되고 있는지 확인

## 2. 데이터 구조 선택
- 적절한 데이터 구조가 선택되었는지 확인 (list vs set vs dict vs deque 등)
- list를 사용하는 경우 슬라이싱이나 인덱싱이 최적화되어 있는지 확인
- dict의 키 조회가 자주 일어나는 경우 set을 사용할 수 있는지 확인
- collections 모듈의 특화된 자료구조(deque, Counter, defaultdict 등) 활용 여부 확인

## 3. 반복문 및 리스트 컴프리헨션
- 불필요한 반복문이 있는지 확인
- map, filter, 리스트 컴프리헨션을 적절히 활용하고 있는지 확인
- 제너레이터(generator)를 사용할 수 있는 경우 제안 (메모리 효율성)
- enumerate, zip 등을 적절히 사용하고 있는지 확인

## 4. 문자열 연산 최적화
- 문자열 연결 시 += 연산자 대신 join() 메서드 사용 여부 확인
- f-string, format(), % 중 적절한 방법 사용 여부
- 정규표현식이 복잡한 경우 컴파일된 패턴 사용 여부

## 5. 함수 호출 및 모듈 임포트
- 불필요한 함수 호출이나 중첩 호출이 있는지 확인
- 모듈 임포트가 함수/클래스 내부에 있는지, 필요할 때만 임포트하는지 확인
- from 모듈 import 함수 형태로 필요한 부분만 임포트하고 있는지 확인

## 6. 메모리 최적화
- 대용량 데이터 처리 시 제너레이터나 이터레이터 활용 여부
- 불필요한 변수나 객체 생성이 있는지 확인
- 메모리 누수 가능성이 있는 순환 참조 확인
- __slots__ 사용 가능 여부 확인 (클래스의 경우)

## 7. I/O 작업 최적화
- 파일 I/O 시 적절한 버퍼링 사용 여부
- 여러 작은 I/O 작업을 한 번에 처리할 수 있는지 확인
- 비동기 I/O(asyncio) 활용 가능 여부
- 데이터베이스 쿼리 최적화 (N+1 문제 등)

## 8. 병렬 처리 및 동시성
- CPU 집약적 작업에 multiprocessing 활용 가능 여부
- I/O 바운드 작업에 asyncio나 threading 활용 가능 여부
- 병렬 처리가 성능 향상에 도움이 되는지 분석

## 9. 캐싱 및 메모이제이션
- 동일한 연산 결과를 재사용할 수 있는지 확인
- functools.lru_cache 활용 가능 여부
- 외부 API 호출이나 데이터베이스 쿼리 결과 캐싱 가능 여부

## 10. 파이썬 특화 최적화
- 내장 함수(built-in functions) 활용 여부
- NumPy, pandas 등 최적화된 라이브러리 활용 가능 여부
- Cython이나 PyPy 사용 고려 필요 여부
- 불필요한 try-except 블록이 성능에 영향을 주는지 확인

## 11. 코드 실행 프로파일링 고려사항
- 병목 구간을 식별하기 위한 프로파일링 도구(cProfile, line_profiler 등) 사용 권장
- 실제 성능 측정 결과를 바탕으로 한 최적화 제안

## 요구사항
- 각 항목별로 구체적인 문제점을 지적하고 개선 방안을 제시해주세요
- 성능 개선 효과가 예상되는 경우 구체적인 수치나 지표를 포함해주세요
- 코드 예시와 함께 개선된 버전을 보여주세요
- 최적화 트레이드오프(가독성 vs 성능)를 고려한 균형잡힌 제안을 해주세요
```



```
** 코드 보안 관점의 리뷰 프롬프트 **

당신은 보안 전문가입니다.
코드를 보안 관점에서 검토하고, 아래 체크리스트를 기반으로 상세한 보고서를 작성해주세요.

### 입력 검증
- [ ] 모든 사용자 입력에 대한 검증이 있는가?
- [ ] SQL 쿼리에서 파라미터화된 쿼리(parameterized queries)를 사용하는가?
- [ ] 쉘 명령어 실행 시 shlex.quote() 또는 subprocess를 안전하게 사용하는가?
- [ ] 파일 경로에서 ../ 같은 경로 조작을 방지하는가?
- [ ] 정규표현식에서 ReDoS (Regular Expression Denial of Service) 취약점이 없는가?

### 인증/인가
- [ ] 비밀번호가 bcrypt, argon2 등 안전한 해싱 알고리즘으로 저장되는가?
- [ ] JWT 토큰 검증이 적절히 이루어지는가?
- [ ] 세션 고정 공격을 방지하는가?
- [ ] 권한 상승 취약점이 없는가?

### 민감 정보
- [ ] 하드코딩된 자격증명이 없는가?
- [ ] .env 파일이나 환경 변수를 사용하는가?
- [ ] 로그에 비밀번호, 토큰, 개인정보가 기록되지 않는가?
- [ ] 디버그 모드가 프로덕션에서 비활성화되어 있는가?

### 암호화
- [ ] 암호화에 강력한 알고리즘을 사용하는가? (AES-256, ChaCha20 등)
- [ ] 랜덤 값 생성에 secrets 모듈을 사용하는가? (random 모듈 대신)
- [ ] IV(Initialization Vector)가 랜덤하게 생성되는가?

### 의존성
- [ ] requirements.txt에 고정된 버전이 명시되어 있는가?
- [ ] 알려진 취약점이 있는 패키지를 사용하지 않는가?
- [ ] 정기적으로 의존성을 업데이트하는가?

### 파일 작업
- [ ] 파일 업로드 시 파일 타입 검증이 있는가?
- [ ] 파일 크기 제한이 있는가?
- [ ] 임시 파일이 사용 후 삭제되는가?
- [ ] 파일 권한이 적절히 설정되는가?

### 네트워크
- [ ] requests 라이브러리 사용 시 verify=True로 SSL 검증을 하는가?
- [ ] 타임아웃이 설정되어 있는가?
- [ ] 외부 URL 검증이 있는가? (SSRF 방지)

### 에러 처리
- [ ] 일반적인 에러 메시지만 사용자에게 노출되는가?
- [ ] 스택 트레이스가 로그에만 기록되는가?
- [ ] 예외 처리가 모든 엔드포인트에서 이루어지는가?

### 코드 실행
- [ ] eval(), exec() 같은 위험한 함수를 사용하지 않는가?
- [ ] pickle.load() 대신 안전한 직렬화 방법을 사용하는가?
- [ ] yaml.load() 대신 yaml.safe_load()를 사용하는가?

### 웹 애플리케이션 특화 (해당되는 경우)
- [ ] CSRF 토큰이 사용되는가?
- [ ] XSS 방지를 위해 입력이 이스케이프되는가?
- [ ] Rate limiting이 구현되어 있는가?
- [ ] CORS 설정이 적절한가?
- [ ] Security headers가 설정되어 있는가? (CSP, X-Frame-Options 등)
```


